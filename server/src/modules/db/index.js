const findUser = (username, passwordHash)=>{
    // check database for username and password hash
    const userId = "apple";
    return(userId);
};


const addUser = (username, passwordHash)=>{
    // create new user in the database
    const userId = "apple";
    return(userId);
};

module.exports = {findUser, addUser};